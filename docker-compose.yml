version: '3.9'

services:
  app: 
    build:
      context: .
      dockerfile: Dockerfile
    # depends_on:
    #   - database
    ports: 
      - "3000:3000"
    volumes:
      - .:/app
      - gem_cache:/usr/local/bundle/gems
      - node_modules:/app/node_modules
    env_file: .env
    environment:
      RAILS_ENV: development
    
  # PostgreSQL database service
  # db:
  #   image: postgres:13
  #   container_name: deals_website_db
  #   restart: always
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: your_postgres_password
  #     POSTGRES_DB: deals_website_production
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data

volumes:
  gem_cache:
  db_data:
  node_modules: 
  # pgdata:
